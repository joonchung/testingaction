name: CI

on: 
  pull_request:
    branches:
      - master

jobs:
  tf_fmt:
    name: format
    runs-on: ubuntu-latest
    steps:
    - name: Terraform format
      uses: hashicorp/terraform-github-actions/fmt@v0.3.7
